<nav class="fixed top-0 left-0 right-0 z-50 bg-indigo-600 text-white shadow-lg">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
            {# Logo / Titre #}
            <div class="flex-shrink-0">
                <a href="{{ path('app_home') }}" class="text-2xl font-bold tracking-tight">
                    Review Sym
                </a>
            </div>

            {# Liens de navigation / Auth #}
            <div class="flex items-center space-x-6">
                {# Liens publics (ex: Accueil, Genres, Livres, etc.) #}
                <a href="{{ path('genre_index') }}" class="hover:text-indigo-200 transition duration-150">
                    Genres
                </a>

                {# Authentification #}
                <div class="flex items-center space-x-4">
                    {% if app.user %}
                        <a href="{{ logout_path() }}"
                           class="px-4 py-2 bg-red-600 hover:bg-red-700 rounded-lg transition duration-200">
                            Déconnexion
                        </a>
                    {% else %}
                        {# Non connecté #}
                        <a href="{{ path('app_login') }}"
                           class="hover:text-indigo-200 transition duration-150">
                            Connexion
                        </a>
                        <a href="{{ path('app_register') }}"
                           class="px-4 py-2 bg-white text-indigo-600 font-medium rounded-lg hover:bg-indigo-50 transition duration-200">
                            S'inscrire
                        </a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</nav>
